{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "seed-db": "sail artisan db:seed --class=ExpensesAndCategoriesSeeder",
        "migrate-db": "sail artisan migrate:fresh"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.2.6"
    }
}
